#!/usr/bin/env fish
set -l path (zenity --file-selection --save --file-filter "Screenshot | *.png" --filename "screenshot.png")
wl-paste -t image/png > $path
notify-send "Saved" "$path"
wl-copy "$path"
