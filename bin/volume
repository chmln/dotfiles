#!/usr/bin/fish
if [ "$argv[1]" = "up" ]
    amixer sset Master 3%+
else
    amixer sset Master 3%-
end

set -l volume (amixer get -M Master | rg % | sd ".*\[(\d+)%\].*" '$1')
bar-notification $volume "apps/gnome-volume-control.svg" 2000
