#!/usr/bin/env dash
path=$(
  /usr/bin/fd -t d -uu '.git$' ~/workspace -x dirname {} \
  | sd "/home/greg/workspace/" ""\
  | rofi -i -dmenu -tokenize -theme-str '#prompt { enabled: false; }'
)
if [ -n "$path" ]; then
  terminal -e fish -c "nvim \"/home/greg/workspace/$path\""
fi
