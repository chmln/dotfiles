#!/bin/sh
# use external monitor and prevent wine glitches
xrandr --output eDP1 --off --output HDMI1 --primary --mode 2560x1440

# disable vsync
vblank_mode=0 wine "$@"

# restore monitor setup
sleep 1
i3-msg "restart"
